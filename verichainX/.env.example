# =================================================================
# Agentic Counterfeit Detection System - Environment Configuration
# =================================================================

# -----------------------------------------------------------------
# Core Application Settings
# -----------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false
LOG_LEVEL=INFO
SECRET_KEY=your-super-secret-key-change-this-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# -----------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------
# PostgreSQL primary database
DATABASE_URL=postgresql://counterfeit_user:secure_password@localhost:5432/counterfeit_detection
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=30

# Redis configuration
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=your_redis_password
REDIS_DB_CACHE=1
REDIS_DB_SESSIONS=2
REDIS_DB_CELERY=3

# -----------------------------------------------------------------
# AI Service Configuration
# -----------------------------------------------------------------
# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=4000
OPENAI_TEMPERATURE=0.1

# Anthropic Claude Configuration
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-haiku-20240307
ANTHROPIC_MAX_TOKENS=4000

# Vector Embeddings Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_PROVIDER=sentence-transformers
VECTOR_DIMENSIONS=384
EMBEDDING_BATCH_SIZE=32

# -----------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------
JWT_SECRET_KEY=your-jwt-secret-key-change-this
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Session configuration
SESSION_SECRET_KEY=your-session-secret-key
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=lax

# Password hashing
BCRYPT_ROUNDS=12

# -----------------------------------------------------------------
# zkSNARK Configuration (Optional - Enterprise Feature)
# -----------------------------------------------------------------
ZKSNARK_ENABLED=false
CIRCUITS_PATH=/opt/circuits
TRUSTED_SETUP_PATH=/opt/trusted-setup
SNARKJS_BINARY_PATH=/usr/local/bin/snarkjs
CIRCOM_BINARY_PATH=/usr/local/bin/circom

# Circuit compilation settings
CIRCUIT_COMPILE_TIMEOUT=300
TRUSTED_SETUP_CEREMONY_PARTICIPANTS=10
PROOF_GENERATION_TIMEOUT=60
PROOF_VERIFICATION_TIMEOUT=10

# -----------------------------------------------------------------
# Enterprise Integration (Optional)
# -----------------------------------------------------------------
# ServiceNow GRC Integration
SERVICENOW_ENABLED=false
SERVICENOW_URL=https://your-instance.service-now.com
SERVICENOW_USERNAME=your_servicenow_username
SERVICENOW_PASSWORD=your_servicenow_password
SERVICENOW_API_VERSION=v1

# SAP GRC Integration
SAP_GRC_ENABLED=false
SAP_GRC_HOST=your-sap-host.com
SAP_GRC_PORT=8000
SAP_GRC_USERNAME=your_sap_username
SAP_GRC_PASSWORD=your_sap_password
SAP_GRC_CLIENT=100

# MetricStream Integration
METRICSTREAM_ENABLED=false
METRICSTREAM_API_URL=https://your-instance.metricstream.com/api
METRICSTREAM_API_KEY=your_metricstream_api_key
METRICSTREAM_ORG_ID=your_organization_id

# -----------------------------------------------------------------
# Blockchain Configuration (Optional)
# -----------------------------------------------------------------
BLOCKCHAIN_ENABLED=false
ETHEREUM_RPC_URL=https://mainnet.infura.io/v3/your-project-id
ETHEREUM_CHAIN_ID=1
ANCHOR_PRIVATE_KEY=your-private-key-for-anchoring
ANCHOR_CONTRACT_ADDRESS=0x1234567890123456789012345678901234567890
GAS_PRICE_GWEI=20
GAS_LIMIT=21000

# Bitcoin anchoring (alternative)
BITCOIN_ENABLED=false
BITCOIN_RPC_URL=https://your-bitcoin-node:8332
BITCOIN_RPC_USERNAME=your_bitcoin_rpc_username
BITCOIN_RPC_PASSWORD=your_bitcoin_rpc_password

# -----------------------------------------------------------------
# Notification Services
# -----------------------------------------------------------------
# Email configuration (SMTP)
SMTP_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@your-company.com
SMTP_FROM_NAME=Counterfeit Detection System

# Slack integration
SLACK_ENABLED=false
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/slack/webhook
SLACK_CHANNEL=#alerts
SLACK_USERNAME=CounterfeitBot
SLACK_EMOJI=:shield:

# SMS notifications (Twilio)
SMS_ENABLED=false
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM_NUMBER=+1234567890

# Push notifications
PUSH_ENABLED=false
FIREBASE_SERVER_KEY=your_firebase_server_key
APNS_KEY_ID=your_apns_key_id
APNS_TEAM_ID=your_apns_team_id

# -----------------------------------------------------------------
# Celery Task Queue Configuration
# -----------------------------------------------------------------
CELERY_BROKER_URL=redis://localhost:6379/3
CELERY_RESULT_BACKEND=redis://localhost:6379/3
CELERY_WORKER_CONCURRENCY=4
CELERY_TASK_SOFT_TIME_LIMIT=300
CELERY_TASK_TIME_LIMIT=600
CELERY_BEAT_SCHEDULE_ENABLED=true

# Task priorities
CELERY_HIGH_PRIORITY_QUEUE=high_priority
CELERY_NORMAL_PRIORITY_QUEUE=default
CELERY_LOW_PRIORITY_QUEUE=low_priority

# -----------------------------------------------------------------
# Agent Configuration
# -----------------------------------------------------------------
# Multi-agent system settings
AGENT_ORCHESTRATOR_ENABLED=true
AGENT_MAX_CONCURRENT_TASKS=10
AGENT_TASK_TIMEOUT=300
AGENT_RETRY_ATTEMPTS=3
AGENT_BACKOFF_FACTOR=2

# Agent-specific settings
AUTHENTICITY_ANALYZER_MODEL=gpt-4o-mini
RULE_ENGINE_ENABLED=true
NOTIFICATION_AGENT_ENABLED=true
ENFORCEMENT_AGENT_ENABLED=true

# -----------------------------------------------------------------
# Monitoring & Observability
# -----------------------------------------------------------------
# Prometheus metrics
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090
METRICS_ENDPOINT=/metrics

# Jaeger tracing
JAEGER_ENABLED=false
JAEGER_AGENT_HOST=localhost
JAEGER_AGENT_PORT=6831
JAEGER_SERVICE_NAME=counterfeit-detection-api

# Sentry error tracking
SENTRY_ENABLED=false
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ENVIRONMENT=production
SENTRY_SAMPLE_RATE=0.1

# Application Performance Monitoring
APM_ENABLED=false
APM_SERVICE_NAME=counterfeit-detection
APM_ENVIRONMENT=production

# -----------------------------------------------------------------
# File Storage Configuration
# -----------------------------------------------------------------
# Local file storage
FILE_STORAGE_TYPE=local
FILE_STORAGE_PATH=/opt/counterfeit-detection/storage
MAX_FILE_SIZE_BYTES=10485760  # 10MB
ALLOWED_FILE_EXTENSIONS=jpg,jpeg,png,pdf,csv,xlsx,json

# AWS S3 storage (alternative)
AWS_S3_ENABLED=false
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_S3_BUCKET_NAME=counterfeit-detection-storage
AWS_S3_REGION=us-west-2
AWS_S3_ENDPOINT_URL=https://s3.us-west-2.amazonaws.com

# Google Cloud Storage (alternative)
GCS_ENABLED=false
GCS_BUCKET_NAME=counterfeit-detection-storage
GCS_CREDENTIALS_PATH=/path/to/gcs-credentials.json

# -----------------------------------------------------------------
# Rate Limiting & Security
# -----------------------------------------------------------------
# API rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=100
RATE_LIMIT_BURST_SIZE=200
RATE_LIMIT_STRATEGY=fixed_window

# IP filtering
IP_WHITELIST_ENABLED=false
IP_WHITELIST=127.0.0.1,10.0.0.0/8,192.168.0.0/16
IP_BLACKLIST_ENABLED=false
IP_BLACKLIST=

# Content Security Policy
CSP_ENABLED=true
CSP_DEFAULT_SRC="'self'"
CSP_SCRIPT_SRC="'self' 'unsafe-inline'"
CSP_STYLE_SRC="'self' 'unsafe-inline'"

# -----------------------------------------------------------------
# Performance & Scaling
# -----------------------------------------------------------------
# Connection pooling
CONNECTION_POOL_SIZE=20
CONNECTION_POOL_MAX_OVERFLOW=30
CONNECTION_POOL_TIMEOUT=30

# Caching configuration
CACHE_TTL_SECONDS=3600
CACHE_MAX_SIZE=1000
CACHE_CLEANUP_INTERVAL=300

# Request timeout settings
REQUEST_TIMEOUT_SECONDS=30
SLOW_QUERY_THRESHOLD_MS=1000

# -----------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------
# Development settings (only for development environments)
RELOAD_ON_CHANGE=false
ENABLE_PROFILING=false
MOCK_AI_RESPONSES=false
MOCK_BLOCKCHAIN_CALLS=false

# Testing configuration
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_counterfeit_detection
TEST_REDIS_URL=redis://localhost:6379/15
RUN_INTEGRATION_TESTS=false

# -----------------------------------------------------------------
# Backup & Recovery
# -----------------------------------------------------------------
# Database backup settings
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM
BACKUP_RETENTION_DAYS=30
BACKUP_STORAGE_PATH=/opt/backups
BACKUP_ENCRYPTION_KEY=your-backup-encryption-key

# -----------------------------------------------------------------
# Compliance & Audit
# -----------------------------------------------------------------
# Audit logging
AUDIT_LOGGING_ENABLED=true
AUDIT_LOG_LEVEL=INFO
AUDIT_LOG_FILE=/var/log/counterfeit-detection/audit.log
AUDIT_LOG_MAX_SIZE=100MB
AUDIT_LOG_BACKUP_COUNT=10

# Data retention policies
DATA_RETENTION_DAYS=2555  # 7 years for compliance
ANALYTICS_RETENTION_DAYS=365
LOG_RETENTION_DAYS=90

# Privacy settings
ANONYMIZE_PII=true
ENCRYPT_SENSITIVE_DATA=true
GDPR_COMPLIANCE_MODE=true

# -----------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------
# Enable/disable major features
BRAND_REGISTRATION_ENABLED=true
ZKSNARK_PROOFS_ENABLED=false
ENTERPRISE_INTEGRATION_ENABLED=false
ADVANCED_ANALYTICS_ENABLED=true
REAL_TIME_MONITORING_ENABLED=true
AUTOMATED_ENFORCEMENT_ENABLED=true
KNOWLEDGE_GRAPH_ENABLED=false

# Experimental features
EXPERIMENTAL_FEATURES_ENABLED=false
AI_MODEL_EXPERIMENTS=false
ADVANCED_CACHING_ENABLED=true
PERFORMANCE_OPTIMIZATIONS=true

# -----------------------------------------------------------------
# Environment-Specific Overrides
# -----------------------------------------------------------------
# Use these for environment-specific configurations
ENVIRONMENT=development  # development, staging, production
SERVICE_VERSION=1.0.0
DEPLOYMENT_TIMESTAMP=2024-01-20T10:00:00Z
DEPLOYMENT_COMMIT_SHA=abc123def456